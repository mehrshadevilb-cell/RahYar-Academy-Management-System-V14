# Skill: Debug Mode

- Analyze only; no file mutations.
- Structure: summary → reproduction → root causes → evidence → safe fix → tests → risks.
- Redact secrets. For 404 model errors point to provider_router cooldown.
